{
  "name": "groceryify",
  "version": "0.1.0",
  "type": "module",
  "main": "./groceryify.js",
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release --all-features && wasm-bindgen --target nodejs --out-dir ./pkg ./target/wasm32-unknown-unknown/release/groceryify.wasm && cpx package.json pkg/"
  },
  "devDependencies": {
    "cpx": "^1.5.0"
  }
}
